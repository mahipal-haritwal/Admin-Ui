import React from "react";
import "./Checkbox.css";
import { Flex } from "../Flex";

const Checkbox = () => {
  return (
    <Flex>
      <input type="checkbox" />
    </Flex>
  );
};

export default Checkbox;
